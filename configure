#!/bin/sh

#--------------------------------------------------------------------------#

fmt="%-20s ..."

#--------------------------------------------------------------------------#

printf "$fmt" "version"
version=`cat VERSION`
echo " $version"

#--------------------------------------------------------------------------#

printf "$fmt" "generating Makefile"
rm -f Makefile
sed -e "s,@VERSION@,$version,g" Makefile.in > Makefile
echo " done"
